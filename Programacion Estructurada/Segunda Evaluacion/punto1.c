#include <stdio.h>

#include <conio.h>
int main(void){
    int variable=5;
    int resultado;
    resultado=(variable>=5);
    printf("el resultado es %d \n", resultado);// en pantalla aparece un 1
getch();
    return (0);
} 